{% extends 'base.html.twig' %}

{% block title %}Hello ModificationController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="container-fluid">
<h1 class="mt-2" style="font-family:bold;letter-spacing:2px;"></h1>
<table class="table table-striped  mt-5 " >
<thead>
   <tr class="" style="background-color: #00bcd4; color:#fff; border-bottom:none;">
     <th>TITRE</th>
     <th>CONTENU</th>
     <th>IMAGES</th>
     <th>DATES</th>
     <th>ACTIONS</th>
   </tr>
</thead>

 <tbody>
 {%  for article in articles %}
   <tr>
       <td> {{ article.titre }} </td>
       <td> {{ article.content|u.truncate(70 ,'...') }} </td>
       <td><img src=" /images/blog/{{ article.imageName }} " alt="..." style="width:20%; height:10vh;"> </td>
       <td> {{ article.createAt | date ('d/m/y') }} à {{ article.createAt | date ('h:i') }}</td>
      <td><a href="{{ path('delete' , {'id': article.id }  )}} " style="margin:5px;">Delete</a><a href=" {{ path('edit' , {'id': article.id }  )}}  " class="">Update</a></td>
       
    
  
   </tr>
 {% endfor %}
 <tbody>

</table>
</div>
{% endblock %}
